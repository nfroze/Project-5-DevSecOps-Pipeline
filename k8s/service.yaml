apiVersion: v1
kind: Service
metadata:
  name: devsecops-service
  namespace: project5
spec:
  selector:
    app: devsecops-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
